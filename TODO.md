# TODO — 이어서 진행

> 최근 갱신: 2026-07-05 세션. 완료 항목은 아래 "완료됨"으로 이동.

## 완료됨 (2026-07-05)
- ✅ **입력 화면 부제 텍스트 삭제** — `<p class="sub">천안권 대학 8개교…</p>` 제거. 미사용 `.hero .sub` CSS도 정리.
- ✅ **정시 과목별 백분위 입력** — 국어/수학/탐구1/탐구2 개별 입력.
  - 입력한 과목만 골라 **단순 평균**을 내어 기존 `record.pct70`(평균백분위 컷)과 비교(데이터 재전사 없음).
  - `state.jungsiSub`(입력 원본) + `state.jungsi`(평균, 하위 판정/정렬이 쓰는 값). `calcJungsiAvg()`로 재계산, 입력 시 `#js-avg`만 갱신해 포커스 유지. LocalStorage에 과목별 값도 저장/복원.
  - 백분위는 0~100으로 클램프. 가중치는 단순 평균(추후 대학별 반영비율 반영 여지).

## 남은 확장 작업
- **7개교 데이터 확장**: BU·KNU·NSU·KOREATECH·HOSEO2·SMU2·KONGJU2. 같은 파이프라인(render→crop→비전 전사→JSON) 반복. `data/`에 JSON 추가 + `universities.json` 등록만으로 확장(코드 불변).
- **DKU2 확인 2건**: 생명공학부 '환경원예학전공' 학과명 표기 / 율곡혁신칼리지 정시 다군 모집 173명 이상치.

## 향후(선택) 개선 여지
- 정시 과목별 **컷**과 비교(더 정밀): DKU2 정시 PDF엔 과목별 백분위 컷 존재 → 재전사해 과목별 비교. 1차엔 과함.
- 정시 평균백분위 산출에 **대학별 반영비율** 반영(현재는 단순 평균).
